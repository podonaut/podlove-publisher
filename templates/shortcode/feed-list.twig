<table>
	<thead>
    	<tr>
            <th>Feed</th>
        	<th>Enclosure</th>
        </tr>
    </thead>
    <tbody>
    {% for feed in podcast.feeds %}
		{% if feed.discoverable %}
		    <tr>
            	<td>
                    <a href="{{ feed.url|esc_url }}">{{ feed.title|e('html') }}</a>
                </td>
                <td>
                    <span title="{{ feed.asset.fileType.mimeType|e('html_attr') }} ({{ feed.asset.fileType.extension|e('html_attr') }})">
                        {{ feed.asset.title|e('html') }}
                    </span>
                </td>
            </tr>
    	{% endif %}
	{% endfor %}
    </tbody>
</table>
