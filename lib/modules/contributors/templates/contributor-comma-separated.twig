<span class="podlove-contributors">
	{% for contributor in episode.contributors({group: group, role: role}) %}
		{% if contributor.visible %}
			<span>
				{% if option.avatars == "yes" %}
					{{ contributor.image.html({width: 18, height: 18, class: "avatar avatar-" ~ size ~  " photo", alt: "avatar" })|raw }}
				{% endif %}
				<span class="name">{{ contributor.name|e('html') }}</span></span>{% if not loop.last %}, {% endif %}
		{% endif %}
	{% endfor %}
</span>
